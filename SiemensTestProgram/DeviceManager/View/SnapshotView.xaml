<UserControl x:Class="DeviceManager.View.SnapshotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeviceManager.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Button Margin="1" Width="100" Height="25" HorizontalAlignment="Left" Content="Start" Command="{Binding StartCommand}"></Button>
            <Button Margin="1" Width="100" Height="25" HorizontalAlignment="Left" Content="Stop" Command="{Binding StopCommand}"></Button>

            <StackPanel Margin="1,30,1,1"  Orientation="Horizontal">
                <Label Width="70" Content="Resolution: "></Label>
                <ComboBox Height="25" Width="70" HorizontalAlignment="Left" Margin="1" ItemsSource="{Binding Resolutions}" SelectedItem="{Binding SelectedResolution}"></ComboBox>
            </StackPanel>
            <StackPanel Margin="1" Orientation="Horizontal">
                <Label Width="70" Content="Samples: "></Label>
                <TextBox Margin="1" Height="25" Width="70" Text="{Binding Path=NumberOfSamples, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" MaxLength="4">
                    <TextBox.InputBindings>
                        <KeyBinding Command="{Binding SetNumberOfSamplesCommand}" Key="Enter"/>
                    </TextBox.InputBindings>
                </TextBox>
            </StackPanel>
        </StackPanel>

        
    </Grid>
</UserControl>
